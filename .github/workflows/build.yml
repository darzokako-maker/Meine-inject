name: Manual Mapper Pro Derleyici

on: [push]

jobs:
  build:
    runs-on: windows-latest

    steps:
      - name: Kodları Çek
        uses: actions/checkout@v4

      - name: MSVC (C++) Kurulumu
        uses: ilammy/msvc-dev-cmd@v1
        with:
          arch: x64

      - name: Injector ve DLL Derle
        run: |
          cl.exe /EHsc /O2 /std:c++17 /MT main.cpp /link user32.lib /out:MeineInjector.exe
          cl.exe /LD /O2 /std:c++17 /MT cheat.cpp /link user32.lib /out:cheat.dll

      - name: Dosyaları Hazırla
        # PowerShell yerine standart CMD komutlarıyla kopyalayalım
        shell: cmd
        run: |
          mkdir Pro-Set
          copy MeineInjector.exe Pro-Set\
          copy cheat.dll Pro-Set\

      - name: Paketi Yukle
        uses: actions/upload-artifact@v4
        with:
          name: Meine-Hile-Paketi
          path: Pro-Set/
          
